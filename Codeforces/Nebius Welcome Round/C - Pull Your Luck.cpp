/**            ॐ
 *      author : rhsajal_017
**/

#include <bits/stdc++.h>
using namespace std;
#define mod 1000000007
#define int long long
 
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t, k = 1;
    cin >> t;
    while(t--)
    {
        int n, x, p;
        cin >> n >> x >> p;
        int req1 = n - x;
        
        req *= -2;
        int factor = 1 - 4 * 1 * req;
        int r = sqrt(factor);
        if(r * r != factor)

    }
    return 0;
}