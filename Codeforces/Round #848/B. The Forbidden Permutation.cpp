/**            ॐ
 *      author : rhsajal_017
**/

#include <bits/stdc++.h>
using namespace std;
#define mod 1000000007
#define int long long
 
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int tt, k = 1;
    cin >> tt;
    while(tt--)
    {
        int n, m, d;
        cin >> n >> m >> d;
        vector<int> p(n), a(m);
        for(int i = 0; i < n; i++)
            cin >> p[i];
        for(int i = 0; i < m; i++)
            cin >> a[i];
    }
    return 0;
}